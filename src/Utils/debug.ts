import { Position } from "./types";

export const DEBUG = false;
export const SCROLL_DEBUG = false;
export const ENVIRONMENT_DEBUG = false;

export const DEBUG_START_POS: Position | undefined = { x: -270, y: -60 };
